[tool.mypy]
python_version = "3.13"
strict = true
exclude = [
    "^\\.venv/",
]
warn_unreachable = true
pretty = true

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]